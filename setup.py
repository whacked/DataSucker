from distutils.core import setup

setup(name='DataSucker',
        version = '0.0.1',
        description = '',
        packages = ['DataSucker'],
        install_requires = ['flask', 'flask-cors',],
        )
